<!--
    NavigationItem.xaml
    This file defines the XAML structure and styles for a custom navigation item in the TitheSync UI.
    It includes styles for indicators, menu icons, menu text, and menu buttons,
    as well as a ControlTemplate for a RadioButton used as a menu item.
-->

<views:MvxWpfView x:Class="TitheSync.UI.Customs.NavigationItem"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
                  Name="This"
                  d:DesignHeight="auto"
                  d:DesignWidth="auto"
                  mc:Ignorable="d">

    <views:MvxWpfView.Resources>
        <!--
            Indicator Style
            Defines the style for the indicator, which is a visual element displayed on the left side of the menu item.
            The indicator becomes visible and changes color when the menu item is hovered over or selected.
        -->
        <Style x:Key="IndicatorStyle" TargetType="{x:Type Border}">
            <Setter Property="CornerRadius" Value="0 5 5 0" />
            <Setter Property="Background" Value="{DynamicResource PrimaryBlueColor}" />
            <Setter Property="Visibility" Value="Hidden" />
            <Setter Property="Height" Value="35" />
            <Setter Property="Width" Value="4" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ToggleButton}}" Value="True">
                    <Setter Property="Visibility" Value="Visible" />
                    <Setter Property="Background" Value="{DynamicResource TextFourthColor}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ToggleButton}}" Value="True">
                    <Setter Property="Visibility" Value="Visible" />
                    <Setter Property="Background" Value="{DynamicResource PrimaryBlueColor}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!--
            Menu Icon Style
            Defines the style for the menu icon, which is a Path element.
            The icon's color changes when the menu item is hovered over or selected.
        -->
        <Style x:Key="MenuIconStyle" TargetType="{x:Type Path}">
            <Setter Property="Fill" Value="{DynamicResource SecondaryTextColor}" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ToggleButton}}" Value="True">
                    <Setter Property="Fill" Value="{DynamicResource TertiaryBlueColor}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ToggleButton}}" Value="True">
                    <Setter Property="Fill" Value="{DynamicResource PrimaryBlueColor}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!--
            Menu Text Style
            Defines the style for the menu text, which is a TextBlock element.
            The text's color changes when the menu item is hovered over or selected.
        -->
        <Style x:Key="MenuTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextColor}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Regular" />
            <Setter Property="Margin" Value="18,0,0,0" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ToggleButton}}" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource TertiaryBlueColor}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ToggleButton}}" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryBlueColor}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!--
            Menu Button Style
            Defines the style and ControlTemplate for a RadioButton used as a menu item.
            The template includes an indicator, an icon, and a text block.
        -->
        <Style x:Key="MenuButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="{DynamicResource BorderThickness}" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="Width" Value="270" />
            <Setter Property="Height" Value="80" />
            <Setter Property="Padding" Value="0,10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RadioButton}">
                        <Border Padding="{Binding Padding, ElementName=This}"
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60" />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>

                                <!--  Indicator  -->
                                <Border Style="{StaticResource IndicatorStyle}" />

                                <!--  Icon  -->
                                <Path Width="{Binding IconWidth, ElementName=This, FallbackValue=20, TargetNullValue=20}"
                                      Margin="{Binding IconMargin, FallbackValue='0,0,0,0', TargetNullValue='20,0,0,0'}"
                                      Data="{Binding Icon, ElementName=This, FallbackValue={StaticResource Home}, TargetNullValue={StaticResource DefaultIcon}}"
                                      Stretch="Uniform"
                                      Style="{StaticResource MenuIconStyle}" />

                                <!--  Menu Text  -->
                                <TextBlock Grid.Column="1"
                                           Margin="0,0,0,0"
                                           FontSize="14"
                                           Style="{StaticResource MenuTextStyle}"
                                           Text="{Binding Text, ElementName=This, FallbackValue=MenuText, TargetNullValue=MenuText}" />

                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!--  To set IsChecked property to true on default  -->
                            <DataTrigger Binding="{Binding IsSelected, ElementName=This}" Value="True">
                                <Setter Property="IsChecked" Value="True" />
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </views:MvxWpfView.Resources>

    <!--
        Main Grid
        Contains a single RadioButton styled as a menu button.
        The RadioButton is bound to commands and properties defined in the code-behind or ViewModel.
    -->
    <Grid>
        <RadioButton Command="{Binding CommandName, ElementName=This}"
                     GroupName="{Binding GroupName, ElementName=This}"
                     Style="{StaticResource MenuButtonStyle}" />
    </Grid>
</views:MvxWpfView>