<views:MvxWpfView x:Class="TitheSync.UI.Components.SettingsPane"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
                  d:DesignHeight="auto"
                  d:DesignWidth="auto"
                  FontFamily="Raleway"
                  mc:Ignorable="d">
    <Grid Margin="20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="15" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   HorizontalAlignment="Center"
                   FontFamily="Open Sans"
                   Foreground="{DynamicResource TertiaryTextColor}"
                   Text="Appearance" />

        <Border Grid.Row="1"
                Width="400"
                Height="45"
                Margin="20,0"
                HorizontalAlignment="Center"
                Background="{DynamicResource TertiaryBackgroundColor}"
                CornerRadius="18">
            <StackPanel Orientation="Vertical">
                <!--  Start Theme Switcher  -->
                <StackPanel Margin="20,10"
                            HorizontalAlignment="Right"
                            Orientation="Horizontal">
                    <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock Margin="5,0,30,0"
                                   Foreground="{DynamicResource TertiaryTextColor}"
                                   Text="Switch between Light and Dark mode" />
                    </StackPanel>
                    <Path Width="16"
                          Height="16"
                          VerticalAlignment="Center"
                          Data="{DynamicResource Sun}"
                          Fill="{DynamicResource PrimaryYellowColor}"
                          Stretch="UniformToFill" />
                    <ToggleButton x:Name="Themes"
                                  Margin="10,0,10,0"
                                  HorizontalAlignment="Right"
                                  Click="Themes_Click"
                                  IsChecked="False"
                                  Style="{DynamicResource ToggleButtonStyle}" />
                    <Path Width="16"
                          Height="16"
                          VerticalAlignment="Center"
                          Data="{DynamicResource Moon}"
                          Fill="{DynamicResource PrimaryWhiteColor}"
                          Stretch="Fill" />
                </StackPanel>

                <Separator Margin="5,5" Background="{DynamicResource PrimaryBackgroundColor}" />

            </StackPanel>
        </Border>
        <!--  End Theme Switcher  -->
    </Grid>
</views:MvxWpfView>